# https://www.playframework.com/documentation/latest/Configuration

appointment {
}


play {
  modules.enabled += "co.mscp.appointment.config.Module"

  http {
    secret.key = "1234abcds"
    parser.maxMemoryBuffer = 100MB
    errorHandler = co.mscp.mmk2.scala.play.SimpleErrorHandler
  }

  filters {
    enabled += "play.filters.cors.CORSFilter"
    disabled += "play.filters.hosts.AllowedHostsFilter"
    cors {
      pathPrefixes = ["/"]
      allowedOrigins = null
      allowedHttpMethods = ["GET", "POST", "PUT", "DELETE"]
      allowedHttpHeaders = null
    }
  }
}


# Database configuration
# ~~~~~

slick.dbs.default.profile="util.MSCPPostgresProfile$"
slick.dbs.default.db.driver="org.postgresql.Driver"

slick.dbs.default.db.url="jdbc:postgresql://localhost:6451/medicom"
slick.dbs.default.db.user=medicom
slick.dbs.default.db.password=medicom

slick.dbs.default.db.url=${?MEDICOM_DB_URL}
slick.dbs.default.db.user=${?MEDICOM_DB_USER}
slick.dbs.default.db.password=${?MEDICOM_DB_PASS}

play.evolutions.db.default.autoApply=true